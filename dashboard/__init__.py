"""Pacote do dashboard Streamlit (Etapa 4, Secao 3.5.4).

Existe para que os modulos da aba de validacao possam se importar entre si
como pacote (`from dashboard.tab_validacao_hardware import render`), em vez
de dependerem de o diretorio do script estar no sys.path.
"""
